asgiref==3.11.1
Django==6.0.3
django-unfold==0.100.0
pillow==12.1.1
# 1.4.0 ships prebuilt manylinux wheels for cp314 (Render's default Python).
# Older lines (e.g. 0.18.0) have no cp314 wheel and fall back to compiling
# against the host libheif, which is too old on Render and fails the build.
pillow-heif==1.4.0
python-decouple==3.8
python-slugify==8.0.4
sqlparse==0.5.5
text-unidecode==1.3
tzdata==2025.3
whitenoise==6.12.0
gunicorn==21.2.0
psycopg[binary]
dj-database-url==2.3.0
cloudinary==1.41.0
django-cloudinary-storage==0.3.0
requests==2.34.2
